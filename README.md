# Docker
🏡 Dockerized Streamlit Housing Price Prediction App

🐳 A fully Dockerized Streamlit application for predicting California housing prices using a trained Machine Learning model.
📦 Includes a preprocessing pipeline and a Random Forest model saved as .pkl files.
🚀 Runs consistently on any system using Docker, without needing Python or local dependencies.
💻 Simply build the Docker image and run the container to launch the Streamlit UI at http://localhost:8501
